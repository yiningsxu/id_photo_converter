# 证件照打印排版转换器

这是一个本地运行的 Python + HTML 工具。它可以自动识别上传证件照的常见成品尺寸，把照片裁剪成指定证件照比例，并按指定纸张尺寸自动计算一张纸上能放下的最大张数，然后导出可打印 PNG 和 PDF。

## 功能

- 上传 JPEG/JPG、PNG、WebP、BMP、TIFF、GIF、HEIC/HEIF 等常见图片格式。
- 自动识别常见证件照成品尺寸，也可从预设规格中一键选择。
- 预设包含日本护照/My Number、美国护照/签证、中国大陆护照/签证、日本在留卡、日本驾照、中国大陆居民身份证等常用尺寸。
- 识别不确定时仍可手动输入证件照成品尺寸。
- 输入打印纸尺寸，例如 postcard `100 mm × 148 mm`。
- 自动按 DPI 换算打印像素，默认 `300 DPI`。
- 原图比例与目标比例不一致时，先在原图像素上裁剪，不做拉伸或挤压。
- 通过水平 / 垂直裁剪中心滑块调整裁剪位置。
- 自动计算最佳排版，例如 `2 × 3`、`2 × 2`。
- 输出裁剪预览图、打印纸排版预览图。
- 下载可打印 `PNG` 和精确纸张尺寸的 `PDF`。

## 安装与运行

```bash
cd id_photo_converter
python -m venv .venv
source .venv/bin/activate      # Windows: .venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

然后在浏览器打开：

```text
http://127.0.0.1:5000
```

## 参数说明

| 参数 | 说明 |
|---|---|
| 自动识别上传图尺寸 | 默认开启。优先读取图片 DPI 元数据；没有可靠 DPI 时，根据常见证件照规格和像素尺寸匹配。 |
| 常用规格预设 | 选择后会自动填写证件照宽度 / 高度，并切换为该预设尺寸。 |
| 证件照宽度 / 高度 | 单张证件照的最终物理尺寸，单位 mm。 |
| 打印纸宽度 / 高度 | 打印纸物理尺寸，单位 mm。Postcard 可填 `100 × 148`。 |
| DPI | 输出分辨率。常用打印建议 `300`。 |
| 页边距 | 排版时保留的最小边距，单位 mm。 |
| 照片间距 | 相邻证件照之间的间距，单位 mm。 |
| 自动选择纸张横竖方向 | 允许输出纸张从 `100 × 148` 自动切换为 `148 × 100`，以容纳更多张。 |
| 允许单张证件照旋转 | 允许每张证件照在纸上旋转 90°。剪下后再转正即可；不希望照片横放时请关闭。 |
| 显示裁切边框 | 给每张照片画一圈 1 px 的裁切参考线。 |

## 证件照尺寸预设

| 尺寸 | 适用证件 |
|---|---|
| `35 × 45 mm` | 日本护照/My Number、韩国护照/居民登记证、台湾护照/身分证、法国/德国/意大利等多数欧洲证件 |
| `51 × 51 mm` | 美国护照/签证/DV/USCIS 常见照片 |
| `33 × 48 mm` | 中国大陆护照/旅行证/中国签证 |
| `30 × 40 mm` | 日本在留卡/入管提交照 |
| `24 × 30 mm` | 日本驾照 |
| `26 × 32 mm` | 中国大陆居民身份证、西班牙 DNI/护照常用竖版 |

## 打印注意事项

优先下载 PDF 打印。打印时必须选择：

- `Actual size` / `实际大小` / `100%`
- 不要选择 `Fit to page` / `适合页面`
- 不要让打印机驱动自动缩放

如果打印机不支持无边距 postcard 打印，请在页面里设置足够的页边距，例如 `3 mm` 到 `5 mm`。

## 核心算法

1. 尝试从图片 DPI 元数据或常见证件照像素规格识别成品尺寸。
2. 根据目标证件照宽高比计算裁剪框。
3. 在原始图像上裁剪，避免横向或纵向拉伸。
4. 根据 `mm / 25.4 × DPI` 把目标物理尺寸转换为像素。
5. 对裁剪后的照片进行等比例重采样，得到单张打印尺寸。
6. 根据纸张尺寸、页边距、照片间距计算可放置的最大列数和行数。
7. 将所有照片居中排布到白色画布。
8. 输出带 DPI 元数据的 PNG，并生成同尺寸 PDF 页面。

## 限制

- 自动识别基于 DPI 元数据、常见规格和像素尺寸，无法可靠判断时会保留手动尺寸。
- 这个版本不做人脸检测或自动居中，只使用裁剪中心滑块调整裁剪位置。
- 单个上传文件上限为 `80 MB`。
- 浏览器中的预览图会缩小显示，不代表屏幕上的物理尺寸；下载的 PDF / PNG 才是打印输出。
- 生成的文件会保存到项目目录的 `outputs/`，可定期手动清理。
